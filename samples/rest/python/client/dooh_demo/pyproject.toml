[project]
name = "dooh-demo-client"
version = "0.1.0"
description = "DOOH Screen Buying Demo Client for UCP"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.26.0",
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
